import { afterEach,describe,expect,it,vi } from "vitest";
import { QbitClient } from "../src/qbittorrent.js";
import { loadConfig } from "../src/config.js";

afterEach(()=>vi.unstubAllGlobals());
describe("qBittorrent adapter",()=>{
  it("authenticates, submits a magnet, reads state, and sends controls",async()=>{const calls:Array<{url:string;init:RequestInit}>=[];vi.stubGlobal("fetch",async(input:string|URL|Request,init:RequestInit={})=>{const url=String(input);calls.push({url,init});if(url.endsWith("/auth/login"))return new Response("Ok.",{headers:{"set-cookie":"SID=test-session; HttpOnly; path=/"}});if(url.endsWith("/app/version"))return new Response("5.1.0");if(url.endsWith("/torrents/info"))return Response.json([{hash:"abc",name:"Show.S01E02",state:"downloading",progress:.5,dlspeed:1000,upspeed:20,eta:30,num_seeds:3,num_leechs:2,total_size:100,downloaded:50,save_path:"/downloads/incomplete",content_path:"/downloads/incomplete/Show.S01E02",added_on:1,completion_on:-1}]);return new Response("Ok.");});const client=new QbitClient(loadConfig({qbitUrl:"http://qbit.test",qbitUsername:"admin",qbitPassword:"secret"}));await client.health();await client.addMagnet("magnet:?xt=urn:btih:abc","tv");expect((await client.list())[0]).toMatchObject({hash:"abc",progress:.5});await client.action("abc","pause");expect(calls.filter(call=>call.url.endsWith("/auth/login"))).toHaveLength(1);const add=calls.find(call=>call.url.endsWith("/torrents/add"))!;expect((add.init.body as FormData).get("urls")).toBe("magnet:?xt=urn:btih:abc");const stop=calls.find(call=>call.url.endsWith("/torrents/stop"))!;expect(String(stop.init.body)).toBe("hashes=abc");expect(calls.slice(1).every(call=>(call.init.headers as Record<string,string>).cookie==="SID=test-session")).toBe(true);});
});
