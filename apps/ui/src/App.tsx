import { useEffect,useMemo,useState } from "react";import type{ServerStatus,Torrent}from"@harbor/contracts";import{AlertTriangle,Anchor,CheckCircle2,ChevronDown,Clock3,Download,FolderCheck,History,LayoutGrid,MoreHorizontal,Pause,Play,Plus,Search,Settings,SlidersHorizontal,Upload,Wifi}from"lucide-react";import{api,connection}from"./lib/api";import{Pairing}from"./components/Pairing";import{AddTorrent}from"./components/AddTorrent";import{PlanTimer}from"./components/PlanTimer";
const fmtSpeed=(n:number)=>n?`${(n/1e6).toFixed(1)} MB/s`:'—';const fmtSize=(n:number)=>`${(n/1e9).toFixed(1)} GB`;const fmtEta=(n:number|null)=>n===null?'Waiting':n===0?'Done':`${Math.floor(n/60)}m ${n%60}s`;
export function App(){const[paired,setPaired]=useState(!!connection.get()),[items,setItems]=useState<Torrent[]>([]),[status,setStatus]=useState<ServerStatus|null>(null),[adding,setAdding]=useState(false),[query,setQuery]=useState(""),[filter,setFilter]=useState("all"),[error,setError]=useState("");async function refresh(){try{const[t,s]=await Promise.all([api.list(),api.status()]);setItems(t);setStatus(s);setError("")}catch(e){setError(e instanceof Error?e.message:"Disconnected")}}useEffect(()=>{if(!paired)return;refresh();const id=setInterval(refresh,1200);return()=>clearInterval(id)},[paired]);const visible=useMemo(()=>items.filter(t=>(filter==="all"||filter==="active"&&["queued","downloading"].includes(t.status)||filter==="review"&&t.status==="review"||filter==="complete"&&["organized","completed"].includes(t.status))&&t.name.toLowerCase().includes(query.toLowerCase())),[items,filter,query]);if(!paired)return <><Pairing onPaired={()=>setPaired(true)}/><PlanTimer completed={2}/></>;return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="logo"><Anchor/></span><strong>Harbor</strong></div><nav><Nav icon={<LayoutGrid/>} label="Downloads" active count={items.filter(x=>x.status==="downloading").length}/><Nav icon={<AlertTriangle/>} label="Needs review" count={items.filter(x=>x.status==="review").length}/><Nav icon={<History/>} label="History"/><Nav icon={<FolderCheck/>} label="Library"/><div className="nav-rule"/><Nav icon={<Settings/>} label="Settings"/></nav><div className="server-card"><div><span className={error?"status-dot offline":"status-dot"}/><strong>{connection.get()?.serverName}</strong></div><small>{error?"Connection interrupted":`${status?.engine??"…"} engine · Online`}</small><div className="server-stats"><span><Download/> {fmtSpeed(status?.downloadSpeed??0)}</span><span><Upload/> {fmtSpeed(status?.uploadSpeed??0)}</span></div></div></aside><main className="content"><header className="topbar"><div><p className="eyebrow">YOUR SERVER</p><h1>Downloads</h1></div><div className="top-actions"><button className="connection"><Wifi/> Unraid connected</button><button className="primary" onClick={()=>setAdding(true)}><Plus/> Add torrent</button></div></header><section className="summary"><div><small>ACTIVE</small><strong>{items.filter(x=>x.status==="downloading").length}</strong><span>transfers</span></div><div><small>DOWNLOADING</small><strong>{fmtSpeed(status?.downloadSpeed??0)}</strong><Download/></div><div><small>UPLOADING</small><strong>{fmtSpeed(status?.uploadSpeed??0)}</strong><Upload/></div><div><small>NEEDS REVIEW</small><strong>{items.filter(x=>x.status==="review").length}</strong><AlertTriangle/></div></section><section className="toolbar"><div className="tabs">{[["all","All"],["active","Active"],["complete","Completed"],["review","Needs review"]].map(([id,label])=><button className={filter===id?"active":""} onClick={()=>setFilter(id!)} key={id}>{label}</button>)}</div><div className="tools"><label className="search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search downloads"/></label><button className="icon-button"><SlidersHorizontal/></button></div></section>{error&&<div className="offline-banner"><AlertTriangle/> {error}. Harbor will reconnect automatically.</div>}<section className="torrent-list"><div className="list-head"><span>NAME</span><span>SIZE</span><span>PROGRESS</span><span>SPEED</span><span>ETA</span><span/></div>{visible.length?visible.map(t=><TorrentRow key={t.id} torrent={t} refresh={refresh}/>):<Empty add={()=>setAdding(true)} filtered={!!query||filter!=="all"}/>}</section></main>{adding&&<AddTorrent close={()=>setAdding(false)} added={refresh}/>}<PlanTimer completed={3}/></div>}
function Nav({icon,label,active,count}:{icon:React.ReactNode;label:string;active?:boolean;count?:number}){return <button className={active?"nav-item active":"nav-item"}>{icon}<span>{label}</span>{!!count&&<b>{count}</b>}</button>}
function TorrentRow({torrent:t,refresh}:{torrent:Torrent;refresh:()=>void}){const[actionOpen,setActionOpen]=useState(false);async function act(action:string){setActionOpen(false);await api.action(t.id,action);refresh()}return <article className="torrent-row"><div className="torrent-name"><div className={`file-icon ${t.classification.category}`}><Download/></div><div><strong>{t.name}</strong><p><span className="category">{t.classification.category}</span><span>to {t.destination}</span><span>{t.seeds} seeds · {t.peers} peers</span></p></div></div><span>{fmtSize(t.size)}</span><div className="progress"><div><i style={{width:`${Math.round(t.progress*100)}%`}}/></div><p><b>{Math.round(t.progress*100)}%</b><span>{fmtSize(t.downloaded)} of {fmtSize(t.size)}</span></p></div><div className="speed"><strong>{fmtSpeed(t.downloadSpeed)}</strong><small>↑ {fmtSpeed(t.uploadSpeed)}</small></div><span className="eta">{t.status==="review"?<em>Needs review</em>:t.status==="organized"?<em className="complete"><CheckCircle2/> Organized</em>:fmtEta(t.etaSeconds)}</span><div className="row-actions"><button className="icon-button" title={t.status==="paused"?"Resume":"Pause"} onClick={()=>act(t.status==="paused"?"resume":"pause")}>{t.status==="paused"?<Play/>:<Pause/>}</button><button className="icon-button" onClick={()=>setActionOpen(!actionOpen)}><MoreHorizontal/></button>{actionOpen&&<div className="action-menu"><button onClick={()=>act("retry")}>Retry</button><button className="danger" onClick={async()=>{if(confirm("Remove this torrent from Harbor? Downloaded data will be kept.")){await api.remove(t.id);refresh()}}}>Remove torrent</button></div>}</div></article>}
function Empty({add,filtered}:{add:()=>void;filtered:boolean}){return <div className="empty"><div className="empty-rings"><Anchor/></div><h2>{filtered?"Nothing matches":"Your harbor is calm"}</h2><p>{filtered?"Try changing the search or filter.":"Send a torrent to Unraid and it will appear here with live progress."}</p>{!filtered&&<button className="primary" onClick={add}><Plus/> Add your first torrent</button>}</div>}
