import { useEffect,useState } from "react";import { Check,ChevronUp,Clock3 } from "lucide-react";
const started=Date.now();
const steps=["Architecture & contracts","Secure companion API","Persistent mock engine","Connected desktop interface","Docker & Unraid packaging","Tests & documentation"];
export function PlanTimer({completed}:{completed:number}){const [open,setOpen]=useState(false);const [seconds,setSeconds]=useState(0);useEffect(()=>{const id=setInterval(()=>setSeconds(Math.floor((Date.now()-started)/1000)),1000);return()=>clearInterval(id)},[]);const time=`${String(Math.floor(seconds/60)).padStart(2,"0")}:${String(seconds%60).padStart(2,"0")}`;return <aside className={`plan-timer ${open?"open":""}`}><button className="plan-head" onClick={()=>setOpen(!open)}><span className="plan-pulse"><Clock3 size={15}/></span><span><strong>Build plan</strong><small>{completed} of {steps.length} · {time}</small></span><ChevronUp size={15}/></button>{open&&<div className="plan-list">{steps.map((step,i)=><div className={i<completed?"done":i===completed?"current":""} key={step}><span>{i<completed?<Check size={12}/>:i+1}</span>{step}</div>)}</div>}</aside>}
