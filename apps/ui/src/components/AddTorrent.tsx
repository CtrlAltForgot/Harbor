import { useState } from "react";import { AlertCircle,ChevronDown,HardDrive,Link2,Server,X } from "lucide-react";import type { Category,RetentionPolicy } from "@harbor/contracts";import { api } from "../lib/api";
export function AddTorrent({close,added}:{close:()=>void;added:()=>void}){const [magnet,setMagnet]=useState(""),[category,setCategory]=useState<Category>("auto"),[retention,setRetention]=useState<RetentionPolicy>("seed"),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(){setBusy(true);setError("");try{await api.add({magnet,category,retention,mode:"server"});added();close()}catch(e){setError(e instanceof Error?e.message:"Could not add torrent")}finally{setBusy(false)}}return <div className="scrim" onMouseDown={e=>e.target===e.currentTarget&&close()}><section className="sheet"><header><div><p className="eyebrow">NEW TRANSFER</p><h2>Add to Harbor</h2></div><button className="icon-button" onClick={close}><X/></button></header><div className="destination-banner"><span><Server size={19}/></span><div><strong>Downloading on Unraid</strong><small>Files will not be saved to this computer</small></div><button>Server mode</button></div><label>Magnet link<div className="input-icon"><Link2 size={17}/><textarea autoFocus value={magnet} onChange={e=>setMagnet(e.target.value)} placeholder="magnet:?xt=urn:btih:…"/></div></label><div className="field-grid"><label>Category<div className="select-wrap"><select value={category} onChange={e=>setCategory(e.target.value as Category)}>{["auto","movie","tv","game","music","software","book","general"].map(x=><option value={x} key={x}>{x==="auto"?"Detect automatically":x[0]!.toUpperCase()+x.slice(1)}</option>)}</select><ChevronDown/></div></label><label>After organization<div className="select-wrap"><select value={retention} onChange={e=>setRetention(e.target.value as RetentionPolicy)}><option value="seed">Keep seeding</option><option value="stop">Stop torrent</option><option value="remove">Remove torrent, keep files</option><option value="ask">Ask me</option></select><ChevronDown/></div></label></div><div className="safe-note"><HardDrive size={17}/><span><strong>Destination is verified before organization.</strong> Original data is preserved until the library copy passes verification.</span></div>{error&&<div className="error"><AlertCircle size={16}/>{error}</div>}<footer><button className="quiet" onClick={close}>Cancel</button><button className="primary" disabled={!magnet||busy} onClick={submit}>{busy?"Adding…":"Start on server"}</button></footer></section></div>}
