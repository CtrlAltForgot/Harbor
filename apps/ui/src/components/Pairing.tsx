import { useState } from "react";import { Anchor,ArrowRight,LockKeyhole,Server } from "lucide-react";import { api } from "../lib/api";
export function Pairing({onPaired}:{onPaired:()=>void}){const [url,setUrl]=useState("http://localhost:7331"),[code,setCode]=useState("harbor-local"),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function pair(){setBusy(true);setError("");try{await api.pair(url,code);onPaired()}catch(e){setError(e instanceof Error?e.message:"Could not pair")}finally{setBusy(false)}}return <main className="pair-page"><div className="pair-brand"><div className="logo"><Anchor/></div><span>Harbor</span></div><section className="pair-card"><div className="pair-art"><div className="server-orbit"><Server size={30}/><i/><i/></div></div><p className="eyebrow">WELCOME ABOARD</p><h1>Connect to your Harbor</h1><p className="lede">Pair this desktop with the companion running on your Unraid server. Downloads stay on your server, where they belong.</p><label>Server address<input value={url} onChange={e=>setUrl(e.target.value)} placeholder="http://tower.local:7331"/></label><label>Pairing code<div className="input-icon"><LockKeyhole size={17}/><input value={code} onChange={e=>setCode(e.target.value)} onKeyDown={e=>e.key==="Enter"&&pair()} /></div></label>{error&&<div className="error">{error}</div>}<button className="primary" onClick={pair} disabled={busy}>{busy?"Connecting…":"Connect securely"}<ArrowRight size={17}/></button><p className="privacy"><LockKeyhole size={13}/> Credentials remain on this device. Harbor is LAN-only by default.</p></section></main>}
